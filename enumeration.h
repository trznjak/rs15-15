#ifndef ENUMERATION
#define ENUMERATION

enum MODE {
    DEFAULT,
    STATE,
    TRANSITION,
    DELETE
};

enum DRAW_SHAPE {
    DASHED,
    NORMAL,
};

enum INPUT_TYPE {
    GRAPH,
    LINECOMMAND,
    FILE_I
};

#endif // ENUMERATION

